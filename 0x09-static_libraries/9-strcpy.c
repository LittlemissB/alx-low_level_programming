#include <string.h>
/**
 * _strcpy - function that copies the string from one pointer to another
 * @dest: destination of string
 * @src: source of string to copy
 *
 * Return: pointer to dest
 */
char *_strcpy(char *dest, char *src)
{
	return (strcpy(dest, src));
}
